COHESION COACH — iPhone-ready PWA

This version is designed to be hosted at an HTTPS web address.
Once hosted, open the URL in Safari and choose Share > Add to Home Screen > Open as Web App.

Files:
index.html — app
manifest.webmanifest — web-app metadata
sw.js — offline caching
icon.svg — Home Screen icon

The app stores progress locally in the browser.
